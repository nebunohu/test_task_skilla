import { FC } from 'react';

// Styles
import styles from './menu.module.css';

const Menu: FC = () => {
  return (
    <aside className={`${styles.wrapper}`}>
        Menu
    </aside>
  );
};

export default Menu;